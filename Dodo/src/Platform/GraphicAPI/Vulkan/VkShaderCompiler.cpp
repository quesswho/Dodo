#include "VkShaderCompiler.h"

namespace Dodo::Platform {

    uint VkShaderCompiler::Compile(const ShaderSource& source)
    {
        return 0;
    }

    uint VkShaderCompiler::CompileStage(const std::string& source)
    {
        return 0;
    }
} // namespace Dodo::Platform
