#include "VulkanShaderCompiler.h"

namespace Dodo::Platform {

    uint VulkanShaderCompiler::Compile(const ShaderSource& source)
    {
        return 0;
    }

    uint VulkanShaderCompiler::CompileStage(const std::string& source)
    {
        return 0;
    }
} // namespace Dodo::Platform
